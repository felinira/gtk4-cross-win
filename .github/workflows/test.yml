on:
  pull_request:

name: Test container images without deployment
jobs:
  test:
    uses: ./.github/workflows/docker.yml
    permissions:
      contents: read
      packages: write
      # This is used to complete the identity challenge
      # with sigstore/fulcio when running outside of PRs.
      id-token: write
    with:
      should-deploy: false